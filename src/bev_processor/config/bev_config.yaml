# Legacy guard file.
# Use bev_config_auto.yaml or bev_config_manual.yaml instead.
# Keeping this file prevents an older installed copy from surviving a normal
# colcon rebuild and silently supplying obsolete parameters.
bev_processor:
  ros__parameters:
    processor_mode: "legacy_entrypoint_removed"
