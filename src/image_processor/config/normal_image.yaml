# normal_image.launch.py는 camera_driver_node 하나만 실행한다.
camera_driver_node:
  ros__parameters:
    # DepthAI 장치의 내장 렌즈 왜곡 보정 사용 여부.
    # false: 보정하지 않음
    # true: OAK 내부 캘리브레이션으로 보정
    undistort_enabled: true

    # 변경 없는 중간 재발행 노드 없이 카메라 드라이버가 직접 발행한다.
    image_topic: "/image/normal"
