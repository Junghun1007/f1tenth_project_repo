auto_control:
  ros__parameters:
    # true이면 VESC 연결, 최신 ERPM, 유효 중앙선이 모두 준비되는 즉시 출발한다.
    # 주행 중 `ros2 topic pub --once /auto/enabled std_msgs/msg/Bool "data: false"`
    # 로 언제든 duty 0과 중앙 조향으로 전환할 수 있다.
    enabled: true
    enable_topic: "/auto/enabled"
    # ordered_path가 기본이다. 이전 mono8 행 평균 경로는 비교용 image 모드다.
    path_input_mode: "ordered_path"
    lane_topic: "/camera/image_bev_lane"
    lane_path_topic: "/camera/path_bev_lane"
    lane_left_boundary_path_topic: "/camera/path_bev_lane_left"
    lane_right_boundary_path_topic: "/camera/path_bev_lane_right"
    measured_erpm_topic: "/vesc/measured_erpm"
    connection_status_topic: "/vesc/connected"
    duty_topic: "/vesc/duty"
    brake_current_topic: "/vesc/brake_current"
    servo_position_topic: "/vesc/servo_position"

    command_duty_topic: "/auto/current_duty"
    command_brake_current_topic: "/auto/current_brake_current"
    target_speed_topic: "/auto/target_speed"
    current_speed_topic: "/auto/current_speed"
    curvature_topic: "/auto/path_curvature"
    steering_angle_topic: "/auto/steering_angle_rad"
    cross_track_error_topic: "/auto/cross_track_error_m"
    heading_error_topic: "/auto/heading_error_rad"
    raw_steering_angle_topic: "/auto/raw_steering_angle_rad"
    command_servo_position_topic: "/auto/current_servo_position"
    planned_path_topic: "/auto/planned_path"

    control_rate_hz: 80.0
    status_log_rate_hz: 2.0
    # 중앙선 수신, 원본 영상 timestamp, ERPM 중 하나라도 오래되면 즉시 duty 0.
    path_timeout_sec: 0.15
    path_capture_maximum_age_sec: 0.20
    erpm_timeout_sec: 0.15

    # bev_processor가 발행하는 120x300 중앙선 mask는 별도의 좌우 margin 없이
    # 원본 BEV와 같은 -0.60..+0.60m, 0..3.0m 좌표계를 사용한다.
    bev_x_max_m: 3.0
    bev_y_max_m: 0.60
    bev_meter_per_pixel: 0.01
    lane_pixel_threshold: 128

    # mono8 중앙선 실측 점열을 전역 다항식으로 변형하지 않는다.
    # 8cm 국소 직선 평활화와 4cm 이상 독립 돌출점 제거만 적용해
    # 먼 코너가 현재 조향 구간을 미리 휘게 하는 현상을 막는다.
    # 급코너에서 중앙선이 짧게 남아도 정지하지 않도록 X=5cm부터
    # 8개 점과 12cm span만 확보되면 경로로 허용한다.
    path_minimum_x_m: 0.05
    path_maximum_x_m: 2.95
    path_minimum_points: 8
    path_minimum_span_m: 0.12
    path_local_smoothing_window_m: 0.08
    path_outlier_threshold_m: 0.04
    path_geometry_window_m: 0.12

    # 양쪽 경계 곡률이 같다고 가정하지 않고 각 경계까지 거리를 독립 측정한다.
    local_path_planner_enabled: true
    local_path_expected_lane_width_m: 0.65
    local_path_vehicle_width_m: 0.30
    local_path_vehicle_wheelbase_m: 0.324
    local_path_safety_margin_m: 0.08
    local_path_minimum_half_width_m: 0.24
    local_path_maximum_half_width_m: 0.48
    local_path_width_smoothing_window_m: 0.20
    local_path_maximum_center_correction_m: 0.08
    local_path_resample_interval_m: 0.02
    local_path_corner_curvature_threshold_per_m: 0.45
    local_path_corner_curvature_smoothing_window_m: 0.16
    local_path_corner_minimum_heading_change_deg: 20.0
    # 코너 시작 25cm 전까지 바깥 위치에 도달하도록 그보다 approach
    # 길이만큼 더 앞에서 이동을 시작하고, 바깥 위치를 유지한 뒤 turn-in한다.
    local_path_corner_approach_length_m: 0.35
    # 0이면 기존 approach 값을 전환 거리로 사용한다. 0보다 크면 이 값이
    # 우선되어 기존 테스트 YAML을 유지하면서 새 이름으로 조정할 수 있다.
    local_path_corner_setup_transition_length_m: 0.0
    local_path_corner_pre_turn_outside_hold_m: 0.25
    # 코너가 전방 BEV에서 사라져도 주행거리 50cm 동안 바깥 위치를
    # 기억한다. 이후 exit 길이에 걸쳐 중앙으로 천천히 복귀한다.
    local_path_corner_exit_length_m: 0.40
    local_path_post_corner_offset_hold_distance_m: 0.50
    # 같은 방향 다음 코너가 1.5m 이내면 중간에 중앙으로 복귀하지 않고
    # 이전 코너의 바깥 탈출 위치를 다음 코너의 바깥 진입과 연결한다.
    local_path_same_direction_corner_link_maximum_gap_m: 1.50
    local_path_outside_offset_fraction: 0.25
    local_path_apex_offset_fraction: 0.35
    local_path_maximum_offset_m: 0.08
    local_path_maximum_offset_slope: 0.70
    # 실차 검증 전 보수적으로 계산 오프셋의 20%만 적용한다.
    local_path_racing_line_weight: 0.20
    local_path_maximum_curvature_per_m: 1.8

    # Stanley: +조향각은 차량 좌표 +Y인 좌회전이다.
    stanley_gain: 1.55
    stanley_softening_speed_mps: 0.35
    # 전역 피팅 없이 실측 중앙선의 12cm 앞 접선을 조향 방향으로 사용한다.
    stanley_heading_lookahead_m: 0.12
    # 선수각 20도 이상의 급코너에서만 방향 guard를 적용하고,
    # 반대 방향 횡오차 보정을 선수각의 90%까지 허용한다.
    stanley_corner_heading_threshold_deg: 20.0
    stanley_corner_opposing_correction_ratio: 0.90
    maximum_steering_angle_deg: 30.0
    # 현재 계산 조향 58%, 직전 명령 42%와 조향 변화율 제한을 함께 적용한다.
    steering_current_weight: 0.58
    steering_rate_limit_deg_per_sec: 240.0
    servo_left: 1.0
    servo_center: 0.46
    servo_right: 0.0
    # 수동주행에서 확인한 실차 서보 링크 방향에 맞춰 최종 출력만 반전한다.
    # /auto/steering_angle_rad의 +는 계속 차량 좌회전 의미를 유지한다.
    steering_servo_inverted: true

    # 곡률의 90 percentile로 순간 한 점의 spike를 무시하면서 코너를 감속한다.
    minimum_speed_mps: 0.80
    maximum_speed_mps: 5.0
    maximum_lateral_acceleration_mps2: 1.2
    curvature_lookahead_minimum_x_m: 0.50
    curvature_lookahead_maximum_x_m: 1.60
    curvature_percentile: 90.0

    # 전방 곡률 제한속도에 backward braking pass와 friction-circle 제한을
    # 적용해 진입 감속 후 apex로 갈수록 제동 여유를 줄인다.
    spatial_speed_profile_enabled: true
    speed_profile_maximum_longitudinal_acceleration_mps2: 1.2
    speed_profile_maximum_longitudinal_deceleration_mps2: 1.8
    speed_profile_minimum_combined_braking_ratio: 0.20
    speed_profile_lookahead_minimum_s_m: 0.05
    speed_profile_lookahead_maximum_s_m: 2.80
    speed_profile_sample_interval_m: 0.04
    speed_profile_acceleration_control_distance_m: 0.20

    # 유효 경로가 생기면 manual_control과 같이 start duty를 즉시 적용한 뒤
    # PID+feed-forward 목표까지 rate limit으로 이동한다. 경로 유실은 즉시 0이다.
    minimum_duty: 0.12
    maximum_duty: 0.20
    duty_rise_rate_per_sec: 0.04
    duty_fall_rate_per_sec: 0.08
    speed_pid_kp: 0.012
    speed_pid_ki: 0.004
    speed_pid_kd: 0.0
    speed_pid_integral_limit: 1.0
    speed_filter_time_constant_sec: 0.05

    # 실제 속도가 목표 속도보다 0.10m/s 이상 높으면 duty를 즉시 버리고
    # COMM_SET_CURRENT_BRAKE로 전환한다. 오차가 0.03m/s 이하가 될 때까지
    # 제동을 유지해 경계에서 duty/브레이크가 반복 전환되는 것을 막는다.
    # 기본 최대 2.5A는 수동 LT의 8A와 VESC 설정 한도보다 보수적인 값이다.
    electrical_brake_enabled: false
    brake_entry_speed_error_mps: 0.10
    brake_exit_speed_error_mps: 0.03
    brake_minimum_vehicle_speed_mps: 0.20
    brake_minimum_current_amps: 1.0
    brake_maximum_current_amps: 2.5
    brake_current_gain_amps_per_mps: 6.0
    brake_current_rise_amps_per_sec: 6.0
    brake_current_fall_amps_per_sec: 16.0

    # Traxxas Slash 4X4 Ultimate 순정 구동계와 기존 camera_driver 값.
    wheel_diameter_m: 0.1095
    motor_pole_pairs: 2
    motor_pinion_teeth: 13
    spur_gear_teeth: 54
    differential_pinion_teeth: 13
    differential_ring_teeth: 37
    erpm_direction_sign: 1.0
    speed_scale_correction: 1.0
